#代码如下

a = 3

#写出输出结果

a>5

a<1

names(mtcars)
#col1 = mtcars$cyl
attach(mtcars)
cyl

# 
hist(mpg) # 频率直方图
barplot(mpg) # 频数直方图
boxplot(mpg) # 画框须图
#

a_mean <- c(1,2,3,4,9,11)
mean(a_mean)  # 求平均值

# 
